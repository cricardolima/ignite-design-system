{
    "name": "@cricardo-design-system/global",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "turbo run dev --parallel",
        "build": "turbo run build",
        "changeset": "changeset",
        "version-packages": "changesets version",
        "release": "turbo run build --filter=@cricardo-design-system/docs && changeset publish"
    },
    "devDependencies": {
        "@changesets/cli": "^2.27.9",
        "turbo": "^2.0.10"
    },
    "packageManager": "npm@10.7.0"
}
